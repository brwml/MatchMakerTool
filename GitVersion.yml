mode: Mainline
next-version: 1.6.0
assembly-versioning-scheme: MajorMinorPatchTag
assembly-file-versioning-scheme: MajorMinorPatchTag
assembly-informational-format: '{AssemblyFileSemVer}.{BranchName}.{ShortSha}'
increment: Patch
branches:
  master:
    regex: ^master$
    increment: Minor
